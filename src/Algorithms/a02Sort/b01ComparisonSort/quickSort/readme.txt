我的想法是这样的,在一个数组里,找到一个基准(为了方便定位第一个),
然后第二个作为left,最后一个作为right.
接下来开始分组过程,right往左移动,期间如果遇到比基准小的值,left和right交换值,同时left右移一位,
right只有遇到比基准大的才会左移,直到两者相遇,最后在right(也就是left的位置)插入基准,完成分区
虽然跟两种标准法都有一点区别,不过也能实现
问题在于插入,好像有点麻烦 复杂度n